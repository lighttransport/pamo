# Embind WASM build (bootstrap-linux.sh)
build/
js/src/
node_modules/

# Comparison-demo WASM build outputs and intermediate cmake tree
wasm/build/
wasm/pamo.mjs
wasm/pamo.wasm
# (parent .gitignore globally ignores `*build*` — un-ignore the script)
!wasm/build.sh

# Node
cli/node_modules/

# CLI run outputs
cli/out/

# Python bytecode
server/__pycache__/
